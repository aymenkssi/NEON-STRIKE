# Neon Strike backend on a VPS that already runs Traefik (no Caddy, no published ports).
# Traefik routes and certifies api.<domain> (API) and <domain> / www (site) through labels.
# Select this file with COMPOSE_FILE=docker-compose.traefik.yml in .env, then `docker compose up -d --build`.
name: neon-strike

services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    volumes:
      - mongo-data:/data/db
      - ./backups:/backups
    networks: [internal]
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: ../backend
    restart: unless-stopped
    depends_on:
      mongo:
        condition: service_healthy
    environment:
      MONGO_URL: mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017/?authSource=admin
      DB_NAME: neon_strike
      PURCHASE_VERIFICATION: ${PURCHASE_VERIFICATION:-google}
      GOOGLE_PLAY_PACKAGE: ${GOOGLE_PLAY_PACKAGE:-com.aymenkssi.neonstrike}
      GOOGLE_SERVICE_ACCOUNT_FILE: /run/secrets/play-service-account.json
      ADMIN_TOKEN: ${ADMIN_TOKEN}
    volumes:
      # Whole folder, not the file: works before the Google key is added.
      - ./secrets:/run/secrets:ro
    networks: [internal, traefik]
    labels:
      - traefik.enable=true
      - traefik.docker.network=${TRAEFIK_NETWORK}
      - traefik.http.routers.neon-api.rule=Host(`${DOMAIN}`)
      - traefik.http.routers.neon-api.entrypoints=${TRAEFIK_ENTRYPOINT}
      - traefik.http.routers.neon-api.tls=true
      - traefik.http.routers.neon-api.tls.certresolver=${TRAEFIK_CERTRESOLVER}
      - traefik.http.routers.neon-api.middlewares=neon-headers,neon-body-limit
      - traefik.http.services.neon-api.loadbalancer.server.port=8001
      - traefik.http.middlewares.neon-headers.headers.stsSeconds=31536000
      - traefik.http.middlewares.neon-headers.headers.contentTypeNosniff=true
      - traefik.http.middlewares.neon-body-limit.buffering.maxRequestBodyBytes=65536

  site:
    image: nginx:1.27-alpine
    restart: unless-stopped
    volumes:
      - ./site:/usr/share/nginx/html:ro
      - ./site-nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks: [traefik]
    labels:
      - traefik.enable=true
      - traefik.docker.network=${TRAEFIK_NETWORK}
      - traefik.http.routers.neon-site.rule=Host(`${SITE_DOMAIN}`) || Host(`www.${SITE_DOMAIN}`)
      - traefik.http.routers.neon-site.entrypoints=${TRAEFIK_ENTRYPOINT}
      - traefik.http.routers.neon-site.tls=true
      - traefik.http.routers.neon-site.tls.certresolver=${TRAEFIK_CERTRESOLVER}
      - traefik.http.routers.neon-site.middlewares=neon-headers
      - traefik.http.services.neon-site.loadbalancer.server.port=80

networks:
  # MongoDB only lives here: never reachable from Traefik or the Internet.
  internal:
  # The network your Traefik container is attached to (see `docker network ls`).
  traefik:
    external: true
    name: ${TRAEFIK_NETWORK}

volumes:
  mongo-data:
